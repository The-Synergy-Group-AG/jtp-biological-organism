
apiVersion: apps/v1
kind: Deployment
metadata:
  name: biological-auth-deployment
  namespace: biological-production
  labels:
    app: biological-organism
    component: biological-auth
spec:
  replicas: 3
  selector:
    matchLabels:
      app: biological-organism
      component: biological-auth
  template:
    metadata:
      labels:
        app: biological-organism
        component: biological-auth
    spec:
      containers:
      - name: biological-auth
        image: biological-organism/biological-auth:latest
        ports:
        - containerPort: 9101
          protocol: TCP
        env:
        - name: ENVIRONMENT
          value: "production"
        - name: SERVICE_PORT
          value: "9101"
        - name: MONITORING_ENABLED
          value: "true"
        resources:
          limits:
            cpu: "1000m"
            memory: "1Gi"
          requests:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 9101
          initialDelaySeconds: 60
          periodSeconds: 30
          timeoutSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 9101
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
        volumeMounts:
        - name: logs-volume
          mountPath: /var/log/biological
      volumes:
      - name: logs-volume
        persistentVolumeClaim:
          claimName: biological-logs-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: biological-auth-service
  namespace: biological-production
  labels:
    app: biological-organism
    component: biological-auth
spec:
  selector:
    app: biological-organism
    component: biological-auth
  ports:
  - name: http
    port: 9101
    targetPort: 9101
    protocol: TCP
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: biological-auth-ingress
  namespace: biological-production
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  tls:
  - hosts:
    - biological-auth.biological-godhood.org
    secretName: biological-auth-tls
  rules:
  - host: biological-auth.biological-godhood.org
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: biological-auth-service
            port:
              number: 9101
